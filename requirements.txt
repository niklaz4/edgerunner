# Core dependencies
dnspython>=2.4.0
requests>=2.31.0
colorama>=0.4.6
tabulate>=0.9.0

# SSL/security related
urllib3>=2.0.0

# Python built-in modules (no installation needed)
# argparse, asyncio, concurrent.futures, json, logging, socket, ssl, sys, time, warnings
# dataclasses, datetime, pathlib, typing, urllib.parse

# Note: Ensure Python 3.8+ for proper typing support
